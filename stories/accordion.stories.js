import React from "react";
import Accordion from "../components/global/accordion/accordion";

export default {
  title: "Accordion",
  component: Accordion,
};

export const Basic = () => <Accordion />;
